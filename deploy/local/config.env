# Deployment Configuration (non-secret, committed to repo)
# All deploy scripts source this file. Change the domain here to propagate everywhere.

DOMAIN=kubertrading.com
SERVER_HOST=quantum            # SSH alias (192.168.1.188)
SERVER_USER=sarbjit            # Remote user
REMOTE_DIR=/opt/kubertrading   # Base directory on server
